import 'package:flutter/widgets.dart';

class V26WebLocalPreview extends StatelessWidget {
  const V26WebLocalPreview({
    super.key,
    required this.fallback,
  });

  final Widget fallback;

  @override
  Widget build(BuildContext context) => fallback;
}
