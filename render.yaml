# Render Blueprint — API ב-https://<שם-השירות>.onrender.com
# Dashboard → New → Blueprint → בחר repo. חובה למלא MONGO_URI + JWT_SECRET.
# Flutter: flutter run --dart-define=VETO_API_BASE=https://veto-app.onrender.com
#
# אם יצרת Web Service ידנית בלי Blueprint: Settings → Root Directory = backend
#   Build: npm ci  |  Start: npm start
# או השאר Root ריק ואז: Build: npm run render-build  |  Start: npm run render-start

services:
  # 1. Backend Service (Node.js API)
  - type: web
    name: veto-app
    runtime: node
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 30d

  # 2. Frontend Service (Flutter Web Static Site)
  - type: web
    name: veto-frontend
    env: static
    region: frankfurt
    plan: free
    rootDir: frontend
    buildCommand: echo "Frontend built locally"
    staticPublishPath: build/web
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
