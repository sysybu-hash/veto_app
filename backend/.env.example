# העתק ל-.env ומלא ערכים
PORT=5001
# Optional: npm run tunnel requests this subdomain first (may fall back to random)
# TUNNEL_SUBDOMAIN=sweet-turkey-60
NODE_ENV=development

# MONGO_URI — רק בקובץ .env המקומי (לא ב-git). ב-Atlas: Connect → העתק connection string.
# דוגמה למבנה (החלף host, user, password, db): mongodb+srv + "://" + user + ":" + password + "@" + cluster + "/dbname?retryWrites=true&w=majority"
MONGO_URI=

JWT_SECRET=your_long_random_secret
JWT_EXPIRES_IN=30d

# CORS — * לפיתוח; בפרודקשן רשום דומיינים מופרדים בפסיקים
CLIENT_ORIGIN=*

# Render / cloud: הגדר את אותם משתנים ב-Dashboard (MONGO_URI, JWT_SECRET, NODE_ENV=production).
# PORT מוגדר אוטומטית — לא חובה ב-.env בפרודקשן.
#
# בדיקות Web בלי SMS: RETURN_OTP_IN_JSON=1 (להסיר כשמחברים SMS אמיתי)
# RETURN_OTP_IN_JSON=1
